import 'package:flutter/material.dart';

const BoxDecoration pinkYellowGradient=BoxDecoration(
  gradient: LinearGradient(
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter,
      colors: [Colors.pink, Colors.yellow]),
);
